import { Routes } from '@angular/router';
import { SetMachineComponent } from './set-machine/set-machine.component';

export const routes: Routes = [
    { path: '', component: SetMachineComponent }
];
